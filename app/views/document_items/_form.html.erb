<%= form_with(model: document_item, local: true) do |form| %>
  <% if document_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document_item.errors.count, "error") %> prohibited this document_item from being saved:</h2>

      <ul>
      <% document_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :document_item_content %>
  </div>

  <div class="field">
    <%= form.label :document_id %>
    <%= form.number_field :document_id, id: :document_item_document_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
