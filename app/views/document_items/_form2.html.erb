<%= form_with(model: document_item,url:"/document_items/create2", local: true) do |form| %>
  <% if document_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document_item.errors.count, "error") %> prohibited this document_item from being saved:</h2>

      <ul>
      <% document_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :質問内容 %>
    <%= form.text_area :content, id: :document_item_content,rows:5,class:"form-control" %>
  </div>

  <div class="field">
    <%= form.hidden_field :document_id%>
  </div>

  <div class="actions">
    <%= form.submit "送信",class:"btn btn-primary" %>
  </div>
<% end %>
