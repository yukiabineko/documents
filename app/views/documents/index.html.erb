<p id="notice"><%= notice %></p>

<h1>提出管理</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>タイトル</th>
      <th>内容</th>
      <th>期限</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @documents.each do |document| %>
      <tr>
        <td><%= document.title %></td>
        <td><%= document.memo %></td>
        <td><%= document.deadline %></td>
        <td><%= link_to '見る', document %></td>
        <td><%= link_to '編集', edit_document_path(document) %></td>
        <td><%= link_to '削除', document, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '選択式作成', new_document_path,class:"btn btn-primary",style:"width:45%;height:40px;" %>
<%= link_to '入力式作成', new2_document_path,class:"btn btn-danger",style:"width:45%;height:40px;" %>

